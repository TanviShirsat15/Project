<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Smart Student Hub</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f3f4f6;
        }
        .container {
            max-width: 420px;
            margin: auto;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .card {
            background-color: #ffffff;
            border-radius: 1.5rem;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }
        .button {
            border-radius: 0.75rem;
        }
    </style>
</head>
<body class="bg-gray-100 p-4">

<div id="app" class="container bg-white rounded-3xl overflow-hidden shadow-2xl">

    <!-- Screen: Login Page -->
    <div id="login-page" class="p-6 flex flex-col justify-center items-center h-full text-center space-y-6">
        <h1 class="text-4xl font-bold text-gray-800">Smart Student Hub</h1>
        <p class="text-gray-500">Your centralized digital portfolio.</p>
        <div class="w-full space-y-4">
            <input type="email" placeholder="University ID" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-colors">
            <input type="password" placeholder="Password" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-colors">
            <button id="login-btn" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 rounded-xl transition-transform transform hover:scale-105">Login</button>
        </div>
        <p class="text-sm text-gray-500">
            <a href="#" class="text-blue-600 hover:underline">Forgot Password?</a>
        </p>
    </div>

    <!-- Screen: Student Dashboard -->
    <div id="student-dashboard" class="hidden p-6">
        <div class="flex items-center justify-between mb-6">
            <div class="flex items-center space-x-3">
                <img src="https://placehold.co/64x64/4a90e2/ffffff?text=U" alt="Profile" class="w-12 h-12 rounded-full border-2 border-blue-500">
                <div>
                    <h2 class="text-lg font-bold text-gray-800">Hi, Jane Doe</h2>
                    <p class="text-sm text-gray-500">Welcome back!</p>
                </div>
            </div>
            <button id="logout-btn" class="text-blue-600 text-sm font-semibold">Logout</button>
        </div>

        <div class="grid grid-cols-2 gap-4 mb-6">
            <div class="card p-4 text-center">
                <h3 class="text-3xl font-bold text-gray-800">12</h3>
                <p class="text-sm text-gray-500">Activities</p>
            </div>
            <div class="card p-4 text-center">
                <h3 class="text-3xl font-bold text-gray-800">3</h3>
                <p class="text-sm text-gray-500">Pending</p>
            </div>
        </div>

        <div class="flex justify-between items-center mb-4">
            <h3 class="text-lg font-semibold text-gray-800">Recent Activities</h3>
            <a href="#" class="text-blue-600 text-sm">See All</a>
        </div>

        <ul class="space-y-3">
            <li class="card p-4 flex items-center justify-between">
                <div>
                    <p class="font-medium">Hackathon Competition</p>
                    <p class="text-xs text-gray-500">SIH 2023</p>
                </div>
                <span class="text-green-500 font-semibold text-sm">Approved</span>
            </li>
            <li class="card p-4 flex items-center justify-between">
                <div>
                    <p class="font-medium">Python Certification</p>
                    <p class="text-xs text-gray-500">Coursera</p>
                </div>
                <span class="text-yellow-500 font-semibold text-sm">Pending</span>
            </li>
            <li class="card p-4 flex items-center justify-between">
                <div>
                    <p class="font-medium">Volunteer Work</p>
                    <p class="text-xs text-gray-500">Green Earth Campaign</p>
                </div>
                <span class="text-green-500 font-semibold text-sm">Approved</span>
            </li>
        </ul>

        <!-- Bottom Navigation Bar -->
        <div class="fixed bottom-0 left-0 right-0 max-w-[420px] mx-auto bg-white border-t rounded-t-3xl shadow-xl p-4 flex justify-around">
            <button class="flex flex-col items-center text-blue-600">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m0 0l-7 7m7-7v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h-2" />
                </svg>
                <span class="text-xs mt-1">Home</span>
            </button>
            <button id="add-activity-btn" class="flex flex-col items-center text-gray-400 hover:text-blue-600 transition-colors">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3m0 0v3m0-3h3m-3 0H9m12 0a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
                <span class="text-xs mt-1">Add</span>
            </button>
            <button id="my-portfolio-btn" class="flex flex-col items-center text-gray-400 hover:text-blue-600 transition-colors">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m4 4v10m8-10v10M4 7v10l8 4m0-10v-4" />
                </svg>
                <span class="text-xs mt-1">Portfolio</span>
            </button>
            <button id="faculty-view-btn" class="flex flex-col items-center text-gray-400 hover:text-blue-600 transition-colors">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.354a4 4 0 110 5.292M12 4.354a4 4 0 100 5.292M12 4.354L15 7.5l-3 3-3-3L12 4.354zm0 0l-3 3m3-3L9 7.5l-3 3m3-3h6M12 13h.01M12 17h.01M12 21h.01M12 17a4 4 0 100 8M12 17a4 4 0 110 8" />
                    <path stroke-linecap="round" stroke-linejoin="round" d="M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
                </svg>
                <span class="text-xs mt-1">Admin</span>
            </button>
        </div>
    </div>

    <!-- Screen: Add New Activity -->
    <div id="add-activity-page" class="hidden p-6">
        <h2 class="text-2xl font-bold mb-6 text-center text-gray-800">Add New Activity</h2>
        <div class="space-y-4">
            <select class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-colors">
                <option value="">Select Activity Type</option>
                <option value="conference">Conference/Workshop</option>
                <option value="certification">Certification</option>
                <option value="club">Club/Volunteering</option>
                <option value="competition">Competition</option>
                <option value="internship">Internship</option>
                <option value="service">Community Service</option>
            </select>
            <input type="text" placeholder="Activity Name" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-colors">
            <input type="date" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-colors">
            <textarea placeholder="Description" rows="4" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-colors"></textarea>
            <label class="block text-sm font-medium text-gray-700">Upload Proof (PDF/Image)</label>
            <input type="file" class="w-full text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-xl file:border-0 file:text-sm file:font-semibold file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100 transition-colors">
            <button id="submit-activity-btn" class="w-full bg-green-600 hover:bg-green-700 text-white font-semibold py-3 rounded-xl transition-transform transform hover:scale-105">Submit for Approval</button>
            <button id="back-to-dashboard-btn" class="w-full bg-gray-200 hover:bg-gray-300 text-gray-700 font-semibold py-3 rounded-xl transition-transform transform hover:scale-105">Cancel</button>
        </div>
    </div>

    <!-- Screen: My Portfolio -->
    <div id="my-portfolio-page" class="hidden p-6">
        <h2 class="text-2xl font-bold mb-6 text-center text-gray-800">My Portfolio</h2>
        <div class="flex justify-center mb-6 space-x-4">
            <button id="download-portfolio-btn" class="flex-1 bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 rounded-xl transition-transform transform hover:scale-105">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 inline-block mr-2" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M3 17a1 1 0 011-1h12a1 1 0 011 1v2a1 1 0 01-1 1H4a1 1 0 01-1-1v-2zm3.293-7.707a1 1 0 011.414 0L10 11.586l2.293-2.293a1 1 0 011.414 1.414l-3 3a1 1 0 01-1.414 0l-3-3a1 1 0 010-1.414z" clip-rule="evenodd" />
                    <path fill-rule="evenodd" d="M10 2a1 1 0 011 1v8.586l2.293-2.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 111.414-1.414L9 11.586V3a1 1 0 011-1z" clip-rule="evenodd" />
                </svg>
                Download PDF
            </button>
            <button id="share-link-btn" class="flex-1 bg-gray-200 hover:bg-gray-300 text-gray-700 font-semibold py-3 rounded-xl transition-transform transform hover:scale-105">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 inline-block mr-2" viewBox="0 0 20 20" fill="currentColor">
                    <path d="M15 8a3 3 0 10-2.977-2.977l-3.22 1.61a3 3 0 00-3.695 0l-3.22-1.61A3 3 0 100 8a3 3 0 002.977 2.977l3.22-1.61a3 3 0 103.695 0l3.22 1.61A3 3 0 1020 8h-3a1 1 0 00-1-1v2a1 1 0 001-1z" />
                </svg>
                Share Link
            </button>
        </div>
        
        <div id="portfolio-list" class="space-y-4">
            <!-- Portfolio items will be dynamically generated here -->
            <div class="card p-4">
                <h3 class="font-bold text-gray-800">Hackathon Competition</h3>
                <p class="text-sm text-gray-500">SIH 2023 | 24th Oct 2023</p>
                <div class="mt-2 text-xs font-semibold text-green-500">Approved</div>
            </div>
            <div class="card p-4">
                <h3 class="font-bold text-gray-800">Python Certification</h3>
                <p class="text-sm text-gray-500">Coursera | 12th Sep 2023</p>
                <div class="mt-2 text-xs font-semibold text-yellow-500">Pending</div>
            </div>
            <div class="card p-4">
                <h3 class="font-bold text-gray-800">Web Development Workshop</h3>
                <p class="text-sm text-gray-500">IEEE Chapter | 15th Aug 2023</p>
                <div class="mt-2 text-xs font-semibold text-green-500">Approved</div>
            </div>
        </div>
        
        <button id="back-from-portfolio-btn" class="w-full mt-6 bg-gray-200 hover:bg-gray-300 text-gray-700 font-semibold py-3 rounded-xl transition-transform transform hover:scale-105">Back</button>
    </div>

    <!-- Screen: Faculty/Admin Dashboard -->
    <div id="faculty-dashboard" class="hidden p-6">
        <div class="flex items-center justify-between mb-6">
            <h2 class="text-2xl font-bold text-gray-800">Admin Dashboard</h2>
            <button id="admin-logout-btn" class="text-blue-600 text-sm font-semibold">Logout</button>
        </div>

        <div class="grid grid-cols-2 gap-4 mb-6">
            <div class="card p-4 text-center">
                <h3 class="text-3xl font-bold text-yellow-500" id="pending-count">1</h3>
                <p class="text-sm text-gray-500">Pending Approvals</p>
            </div>
            <div class="card p-4 text-center">
                <h3 class="text-3xl font-bold text-green-500">12</h3>
                <p class="text-sm text-gray-500">Approved Records</p>
            </div>
        </div>
        
        <h3 class="text-lg font-semibold text-gray-800 mb-4">Pending Records</h3>
        <ul id="pending-list" class="space-y-3">
            <li class="card p-4 flex items-center justify-between" data-id="1">
                <div>
                    <p class="font-medium text-gray-800">John Smith</p>
                    <p class="text-sm text-gray-500">MERN Stack Internship</p>
                </div>
                <button class="bg-blue-600 text-white text-sm font-semibold px-4 py-2 rounded-xl view-details-btn">View Details</button>
            </li>
        </ul>
        
        <button id="back-from-admin-btn" class="w-full mt-6 bg-gray-200 hover:bg-gray-300 text-gray-700 font-semibold py-3 rounded-xl transition-transform transform hover:scale-105">Back to Student View</button>
    </div>
    
    <!-- Screen: Approval Detail Page -->
    <div id="approval-detail-page" class="hidden p-6">
        <h2 class="text-2xl font-bold mb-6 text-center text-gray-800">Record Approval</h2>
        <div class="card p-6 space-y-4">
            <div>
                <h3 class="text-lg font-bold text-gray-800">Student: John Smith</h3>
                <p class="text-sm text-gray-500">ID: 21BCE1234</p>
            </div>
            <div>
                <h4 class="font-bold">Activity:</h4>
                <p>MERN Stack Internship at TechCorp</p>
            </div>
            <div>
                <h4 class="font-bold">Date:</h4>
                <p>15th May 2024 - 15th July 2024</p>
            </div>
            <div>
                <h4 class="font-bold">Description:</h4>
                <p class="text-gray-600">Completed a two-month internship focused on full-stack web development using MongoDB, Express.js, React, and Node.js. Worked on building a dynamic e-commerce platform.</p>
            </div>
            <div>
                <h4 class="font-bold">Proof:</h4>
                <a href="#" class="text-blue-600 hover:underline">View Certificate.pdf</a>
            </div>
        </div>
        <div class="mt-6 flex justify-between space-x-4">
            <button id="approve-btn" class="flex-1 bg-green-600 hover:bg-green-700 text-white font-semibold py-3 rounded-xl transition-transform transform hover:scale-105">Approve</button>
            <button id="reject-btn" class="flex-1 bg-red-600 hover:bg-red-700 text-white font-semibold py-3 rounded-xl transition-transform transform hover:scale-105">Reject</button>
        </div>
        <button id="back-from-approval-btn" class="w-full mt-4 bg-gray-200 hover:bg-gray-300 text-gray-700 font-semibold py-3 rounded-xl transition-transform transform hover:scale-105">Back</button>
    </div>

    <!-- Message Box -->
    <div id="message-box" class="hidden fixed top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 p-6 rounded-2xl shadow-xl z-50 transition-all duration-300 transform scale-95 opacity-0">
        <div class="flex flex-col items-center text-center space-y-4">
            <h3 id="message-title" class="text-xl font-bold text-gray-800"></h3>
            <p id="message-text" class="text-gray-600"></p>
            <button id="close-message-btn" class="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-6 rounded-xl transition-transform transform hover:scale-105">OK</button>
        </div>
    </div>

    <div id="message-overlay" class="hidden fixed inset-0 bg-black opacity-30 z-40"></div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const screens = {
            login: document.getElementById('login-page'),
            studentDashboard: document.getElementById('student-dashboard'),
            addActivity: document.getElementById('add-activity-page'),
            myPortfolio: document.getElementById('my-portfolio-page'),
            facultyDashboard: document.getElementById('faculty-dashboard'),
            approvalDetail: document.getElementById('approval-detail-page'),
        };

        const elements = {
            loginBtn: document.getElementById('login-btn'),
            addActivityBtn: document.getElementById('add-activity-btn'),
            myPortfolioBtn: document.getElementById('my-portfolio-btn'),
            facultyViewBtn: document.getElementById('faculty-view-btn'),
            submitActivityBtn: document.getElementById('submit-activity-btn'),
            backToDashboardBtn: document.getElementById('back-to-dashboard-btn'),
            backFromPortfolioBtn: document.getElementById('back-from-portfolio-btn'),
            adminLogoutBtn: document.getElementById('admin-logout-btn'),
            backFromAdminBtn: document.getElementById('back-from-admin-btn'),
            viewDetailsBtn: document.querySelector('.view-details-btn'),
            approveBtn: document.getElementById('approve-btn'),
            rejectBtn: document.getElementById('reject-btn'),
            backFromApprovalBtn: document.getElementById('back-from-approval-btn'),
            pendingCount: document.getElementById('pending-count'),
            pendingList: document.getElementById('pending-list'),
            messageBox: document.getElementById('message-box'),
            messageOverlay: document.getElementById('message-overlay'),
            messageTitle: document.getElementById('message-title'),
            messageText: document.getElementById('message-text'),
            closeMessageBtn: document.getElementById('close-message-btn'),
        };

        let currentScreen = 'login';
        let pendingRecords = [{
            id: '1',
            student: 'John Smith',
            activity: 'MERN Stack Internship',
            details: 'Completed a two-month internship focused on full-stack web development using MongoDB, Express.js, React, and Node.js. Worked on building a dynamic e-commerce platform.'
        }];

        const showScreen = (screenId) => {
            Object.values(screens).forEach(screen => screen.classList.add('hidden'));
            screens[screenId].classList.remove('hidden');
            currentScreen = screenId;
        };

        const showMessage = (title, text, type) => {
            elements.messageTitle.textContent = title;
            elements.messageText.textContent = text;
            elements.messageBox.style.backgroundColor = type === 'success' ? '#d1f1e2' : type === 'error' ? '#fde5e5' : '#e5f3fd';
            elements.messageTitle.style.color = type === 'success' ? '#21634b' : type === 'error' ? '#9d3232' : '#2d658c';
            elements.messageText.style.color = type === 'success' ? '#21634b' : type === 'error' ? '#9d3232' : '#2d658c';
            
            elements.messageBox.classList.remove('hidden', 'scale-95', 'opacity-0');
            elements.messageOverlay.classList.remove('hidden');
            setTimeout(() => {
                elements.messageBox.classList.remove('scale-95', 'opacity-0');
                elements.messageBox.classList.add('scale-100', 'opacity-100');
            }, 10);
        };

        const hideMessage = () => {
            elements.messageBox.classList.add('scale-95', 'opacity-0');
            elements.messageOverlay.classList.add('hidden');
            setTimeout(() => {
                elements.messageBox.classList.add('hidden');
            }, 300);
        };

        // Event Listeners
        elements.loginBtn.addEventListener('click', () => {
            showScreen('studentDashboard');
        });

        elements.addActivityBtn.addEventListener('click', () => {
            showScreen('addActivity');
        });

        elements.submitActivityBtn.addEventListener('click', () => {
            showMessage('Submitted!', 'Your activity has been submitted for faculty approval.', 'success');
            // Simulate adding a new pending item for the admin view
            pendingRecords.push({ id: crypto.randomUUID(), student: 'Jane Doe', activity: 'New Activity Test', details: 'A test activity submitted from the prototype.' });
            updateAdminDashboard();
            showScreen('studentDashboard');
        });

        elements.backToDashboardBtn.addEventListener('click', () => {
            showScreen('studentDashboard');
        });

        elements.myPortfolioBtn.addEventListener('click', () => {
            showScreen('myPortfolio');
        });

        elements.backFromPortfolioBtn.addEventListener('click', () => {
            showScreen('studentDashboard');
        });

        elements.facultyViewBtn.addEventListener('click', () => {
            showScreen('facultyDashboard');
        });

        elements.backFromAdminBtn.addEventListener('click', () => {
            showScreen('studentDashboard');
        });

        elements.viewDetailsBtn.addEventListener('click', () => {
            showScreen('approvalDetail');
        });

        elements.approveBtn.addEventListener('click', () => {
            showMessage('Approved!', 'The record has been successfully approved.', 'success');
            // Simulate removal from pending list
            const index = pendingRecords.findIndex(rec => rec.student === 'John Smith');
            if (index > -1) {
                pendingRecords.splice(index, 1);
            }
            updateAdminDashboard();
            showScreen('facultyDashboard');
        });

        elements.rejectBtn.addEventListener('click', () => {
            showMessage('Rejected', 'The record has been rejected.', 'error');
            const index = pendingRecords.findIndex(rec => rec.student === 'John Smith');
            if (index > -1) {
                pendingRecords.splice(index, 1);
            }
            updateAdminDashboard();
            showScreen('facultyDashboard');
        });

        elements.backFromApprovalBtn.addEventListener('click', () => {
            showScreen('facultyDashboard');
        });

        elements.closeMessageBtn.addEventListener('click', hideMessage);

        const updateAdminDashboard = () => {
            elements.pendingCount.textContent = pendingRecords.length;
            elements.pendingList.innerHTML = '';
            if (pendingRecords.length > 0) {
                pendingRecords.forEach(record => {
                    const li = document.createElement('li');
                    li.classList.add('card', 'p-4', 'flex', 'items-center', 'justify-between');
                    li.innerHTML = `
                        <div>
                            <p class="font-medium text-gray-800">${record.student}</p>
                            <p class="text-sm text-gray-500">${record.activity}</p>
                        </div>
                        <button class="bg-blue-600 text-white text-sm font-semibold px-4 py-2 rounded-xl view-details-btn" data-id="${record.id}">View Details</button>
                    `;
                    elements.pendingList.appendChild(li);
                });
                // Re-attach event listeners to new buttons
                document.querySelectorAll('.view-details-btn').forEach(btn => {
                    btn.addEventListener('click', () => {
                        showScreen('approvalDetail');
                    });
                });
            } else {
                elements.pendingList.innerHTML = '<li class="text-center text-gray-500 mt-4">No pending records.</li>';
            }
        };

        // Initial setup
        updateAdminDashboard();
        showScreen('login');
    });
</script>

</body>
</html>
